source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in hub_identity_ruby.gemspec.
gemspec

gem 'jwt'
gem 'faraday'

group :development do
  gem 'sqlite3'
  gem 'webmock'
  gem 'pry', '~> 0.13.1'
end

group :test do
  gem 'rails-controller-testing'
end
